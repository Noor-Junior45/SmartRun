<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom URL Scheme Deep Links: smartrun://, in.smartrun.app://, and buildnow:// -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="smartrun" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="in.smartrun.app" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="buildnow" />
            </intent-filter>

            <!-- HTTPS App Links: https://smartrun.in/... and https://www.girirajpower.in/... -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="smartrun.in" />
                <data android:scheme="https" android:host="www.smartrun.in" />
                <data android:scheme="https" android:host="www.girirajpower.in" />
                <data android:scheme="https" android:host="girirajpower.in" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Push Notifications Channel Meta-Data -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="order_updates" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- Android 11+ (API 30+) Package Visibility Queries for Razorpay UPI & Banking Apps -->
    <queries>
        <!-- Generic UPI payment scheme query (enables detecting any installed UPI app) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="credpay" />
        </intent>

        <!-- Popular Indian UPI & Banking Apps so Razorpay can detect and display them -->
        <package android:name="com.google.android.apps.nbu.paisa.user" /> <!-- Google Pay -->
        <package android:name="net.one97.paytm" />                        <!-- Paytm -->
        <package android:name="com.phonepe.app" />                       <!-- PhonePe -->
        <package android:name="in.amazon.mShop.android.shopping" />      <!-- Amazon Pay -->
        <package android:name="in.org.npci.upiapp" />                    <!-- BHIM UPI -->
        <package android:name="com.whatsapp" />                          <!-- WhatsApp Pay -->
        <package android:name="com.csam.icici.bank.imobile" />           <!-- iMobile Pay (ICICI) -->
        <package android:name="com.msf.kfa" />                           <!-- Kotak 811 -->
        <package android:name="com.sbi.upi" />                           <!-- BHIM SBI Pay -->
        <package android:name="com.axis.mobile" />                       <!-- Axis Mobile -->
        <package android:name="com.dreamplug.androidapp" />              <!-- CRED -->
        <package android:name="com.mobikwik_new" />                      <!-- MobiKwik -->
    </queries>
</manifest>
